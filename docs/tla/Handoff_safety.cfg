\* The claim this module exists for: a run that failed for a reason the TREE did
\* not cause never strands the head. Also checks that every state stays within
\* its declared type, that a stand-down only ever follows a mark, and that the
\* mark is never taken back once written.
\*
\* FaultNeverStrandsTheHead is a claim about the WHOLE reachable set of (cause,
\* marked, retry), not about one arm. A mark rule that starts writing a mark for
\* an ending outside the merge fails here even when every call site's own test
\* still passes, because the fault set is derived from that same rule.
\* EXPECT-EXIT: 0
\* EXPECT-DISTINCT: 13
\* SUMMARY: holds — only a merge the resolver could not do latches the next run
INIT Init
NEXT Next
INVARIANT Inv
PROPERTY MarkIsStable
