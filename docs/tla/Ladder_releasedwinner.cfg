\* Non-vacuity witness: a walk both names a winner and releases its attempt
\* mark. claude-run-errored.sh reports zero_cost from the run's cost alone, so a
\* successful run that reached no inference reports zero_cost=true, and
\* evaluate's release rule reads zero_cost without ever reading errored. This is
\* the walk that makes "a released mark implies no winner" false of the shipped
\* policy. Expected: NoReleasedWinner is VIOLATED.
\* EXPECT-EXIT: 12
\* SUMMARY: **violated** — the trace is a zero-billed success, which wins and releases the mark
INIT Init
NEXT Next
INVARIANT NoReleasedWinner
