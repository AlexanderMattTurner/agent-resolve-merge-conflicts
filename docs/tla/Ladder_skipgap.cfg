\* Non-vacuity witness: an error at rung 2 steps OVER an unconfigured rung 3 to
\* rung 4, which has its own credential. run-ladder.py's _slots() drops an
\* unconfigured rung 3..7 before the walk begins, so evaluate never sees the
\* gap. A model that stopped at the gap would prove theorems about a ladder that
\* cannot reach the credentials behind it. Expected: NoSkipOverGap is VIOLATED.
\* EXPECT-EXIT: 12
\* SUMMARY: **violated** — the trace steps from rung 2 over an unset rung 3 to rung 4
INIT Init
NEXT Next
INVARIANT NoSkipOverGap
