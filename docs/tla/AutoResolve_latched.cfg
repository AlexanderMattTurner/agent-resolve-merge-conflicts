\* Witness — a head latched by an attempt mark naming no readable run is
\* reachable, and it is a stall. This is the ending that used to report success.
\* EXPECT-EXIT: 12
\* SUMMARY: witness — the latched stand-down is reachable and reds the run
INIT Init
NEXT Next
INVARIANT NoLatchedStall
