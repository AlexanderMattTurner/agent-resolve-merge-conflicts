\* Non-vacuity witness: a wall-clock-only failure that billed nothing releases
\* the attempt mark. claude-run-errored.sh computes errored, zero_cost,
\* wall_clock_only and content_refusal from independent jq tests, so a shard
\* that died at the wall having reached no inference sets the first three. The walk stops (a fresh
\* credential faces the same wall) AND hands the mark back. Expected:
\* NoReleasedWall is VIOLATED.
\* EXPECT-EXIT: 12
\* SUMMARY: **violated** — the trace is a zero-billed wall-clock failure that releases the mark
INIT Init
NEXT Next
INVARIANT NoReleasedWall
