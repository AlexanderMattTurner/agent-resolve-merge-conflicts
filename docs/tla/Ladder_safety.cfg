\* The safety scheme: the walk never continues past a winner, a released
\* mark never co-occurs with a BILLED run, no rung past rung 2 runs without
\* its own configured credential, rung 2 without one is reached only via
\* a proven zero-cost error at rung 1, and a wall-clock-only failure never
\* advances the walk even into a rung with its own configured credential.
\* Also checks that every state stays within its declared type, and that at
\* most one attempt is ever recorded per rung. Expected: no violation.
\*
\* WinnerImpliesDone and NoAdvanceFromWall restate the emitted table: every
\* Run<i>Ok writes winner and pos = "DONE" in one update, and every
\* Run<i>Errwall writes pos = "DONE". Both are therefore true of any model
\* and prove nothing about the retry policy. They are kept because the table
\* is GENERATED from tests/_ladder_fsm_model.py: a rung whose wall-clock
\* error is retyped to advance stops matching these shells and reds this
\* config. RungRanRequiresConfigured and Rung2NeedsConfiguredOrFreeRetry are
\* different -- each is a claim about the whole WALK, not about one row.
\* ReleasedImpliesNothingBilled is deliberately weaker than "a released mark
\* implies no winner", which is FALSE here: Ladder_releasedwinner is the walk.
\* EXPECT-EXIT: 0
\* EXPECT-DISTINCT: 30,298
\* SUMMARY: holds — winner-once, release-implies-nothing-billed, configured-only rungs, no advance out of a wall-clock-only failure
INIT Init
NEXT Next
INVARIANT Inv
PROPERTY AtMostOneAttempt
