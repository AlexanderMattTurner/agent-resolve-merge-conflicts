\* Non-vacuity witness: every pass has claimed something and a path is still
\* UNCLAIMED. That is exactly the ledger `require_fully_dispositioned` refuses,
\* so this trace is what proves the refusal is not vacuous — the passes can run
\* to the end and leave a file whose bytes git merged with nobody having read
\* them. Expected: NoStuckPath is VIOLATED.
\* EXPECT-EXIT: 12
\* SUMMARY: **violated** — the trace runs every pass and still leaves a path nobody judged
CONSTANT N = 2
INIT Init
NEXT Next
INVARIANT NoStuckPath
