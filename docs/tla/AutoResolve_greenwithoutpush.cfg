\* Witness — a run can push nothing and still report success. A reader would
\* predict the opposite from ConflictStandsImpliesStall, so the counterexample
\* trace is what shows the three endings that name somebody else instead.
\* EXPECT-EXIT: 12
\* SUMMARY: witness — an ending that names who carries the conflict reports success without pushing
INIT Init
NEXT Next
INVARIANT NoGreenWithoutPush
