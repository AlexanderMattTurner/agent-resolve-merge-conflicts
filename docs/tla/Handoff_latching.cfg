\* Witness — a merge the resolver could not do DOES latch the next run, so the
\* safety claim beside this one is not satisfied by never marking anything. The
\* mark is what stops the resolver paying an LLM again for an answer whose inputs
\* did not change, and a spec that only forbade marks would delete that.
\* EXPECT-EXIT: 12
\* SUMMARY: witness — the latching ending is reachable, so the mark still works
INIT Init
NEXT Next
INVARIANT NoLatchingEndingAtAll
