\* Non-vacuity witness: a ladder can spend a billed error at every one of the
\* seven rungs (rungs 2-7 all configured) and reach DONE, keeping its attempt
\* mark the whole way — the paid wall. Expected: NoFullPaidWalk is VIOLATED;
\* the trace runs Run1Errpaid through Run7Errpaid.
\* EXPECT-EXIT: 12
\* SUMMARY: **violated** — the trace is every rung configured and paid, mark kept
INIT Init
NEXT Next
INVARIANT NoFullPaidWalk
