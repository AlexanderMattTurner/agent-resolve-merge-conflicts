\* Witness — a paid run that asks a human to resolve the conflict is a stall.
\* The pull request carries its verdict, and the run still exits non-zero.
\* EXPECT-EXIT: 12
\* SUMMARY: witness — a published handoff verdict is a stall
INIT Init
NEXT Next
INVARIANT NoHandedOffStall
