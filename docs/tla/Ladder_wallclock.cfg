\* Non-vacuity witness: a wall-clock-only failure at rung 3 ends the walk
\* even though rung 4 has its OWN distinct credential configured -- proving
\* the stop is ERR_WALL's doing, not an absent credential's, which is what
\* tells it apart from ERR_PAID (Ladder_paidwall.cfg). Expected:
\* NoWallDespiteConfigured is VIOLATED; the trace runs Run1Ok-alternative
\* rungs up to Run3Errwall with configured4 = TRUE.
\* EXPECT-EXIT: 12
\* SUMMARY: **violated** — the trace is a wall-clock-only failure at rung 3 ending the walk despite rung 4's own configured credential
INIT Init
NEXT Next
INVARIANT NoWallDespiteConfigured
