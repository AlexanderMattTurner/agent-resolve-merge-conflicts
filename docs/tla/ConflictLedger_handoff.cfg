\* Non-vacuity witness: a DEFERRED path really is claimed again by the pass its
\* `to` names, and reaches STAGED. This is prepare deferring a generated region
\* and bundle re-deriving it later. A ledger that only ever refused a second
\* claim would satisfy every safety config beside this one and resolve nothing.
\* Expected: NoHandoff is VIOLATED; the trace defers a path, then stages it from
\* the named pass, which is the step that sets `handed`.
\* EXPECT-EXIT: 12
\* SUMMARY: **violated** — the trace is a deferred path staged by the pass its `to` named
CONSTANT N = 2
INIT Init
NEXT Next
INVARIANT NoHandoff
