\* Non-vacuity witness: the same-token free retry at the rung-1/rung-2
\* boundary is reachable — a proven zero-cost error at rung 1 with no
\* distinct credential configured for rung 2 still runs rung 2. Expected:
\* NoFreeRetry is VIOLATED; the trace is Run1Errzero into Run2*.
\* EXPECT-EXIT: 12
\* SUMMARY: **violated** — the trace is rung 2's same-token retry on a zero-cost error
INIT Init
NEXT Next
INVARIANT NoFreeRetry
