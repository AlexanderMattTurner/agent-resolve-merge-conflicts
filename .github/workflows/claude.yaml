name: Claude

# General-purpose @claude responder. Fires whenever someone mentions @claude in
# an issue, a PR/issue comment, or a PR review, and hands the request to
# claude-code-action, which acts on it (answers, edits code, resolves conflicts)
# and pushes to the branch.
#
# WHY THIS EXISTS — template-sync linkage:
#   template-sync.yaml opens a sync PR and, when a sync hits merge conflicts,
#   posts an "@claude ..." comment asking for help.
#   Without a listener, that comment lands in the void, so a human has to
#   remember to open a Claude session and resolve the conflicts by hand. This
#   workflow IS that listener — it turns the existing @claude comment into a
#   hands-off resolution, leaving only human review + merge.
#
#   IMPORTANT — comment author matters: GitHub does NOT deliver issue_comment
#   events for comments authored by GITHUB_TOKEN (the github-actions[bot]); it's
#   a recursion guard. So for sync auto-resolution to fire, template-sync.yaml's
#   @claude comment must be posted by a real identity — set the
#   TEMPLATE_SYNC_TOKEN secret (a fine-grained PAT) so it doesn't fall back to
#   github.token. The sync flow already needs TEMPLATE_SYNC_TOKEN for its PR to
#   trigger downstream CI, so this is the same existing requirement.
#
# AUTH — two credentials, both already documented in the README secrets table:
#   - Model side: the CLAUDE_CODE_OAUTH_TOKEN_FALLBACK rungs authenticate the
#     Claude Code action, with CLAUDE_CODE_OAUTH_TOKEN as the ladder's last rung.
#     The same tokens the other Claude workflows use; the GitHub App does not cover them.
#   - GitHub side: github_token is TEMPLATE_SYNC_TOKEN (a fine-grained PAT) when
#     set, falling back to GITHUB_TOKEN. The PAT matters because commits pushed
#     under GITHUB_TOKEN are suppressed by the recursion guard above and will NOT
#     re-trigger CI on the PR; a PAT-authored push does. Same pattern as
#     template-sync / phone-home / auto-version.
#
# SETUP (per downstream repo, one time): install the Claude GitHub App
# (https://github.com/apps/claude) and set CLAUDE_CODE_OAUTH_TOKEN (plus
# TEMPLATE_SYNC_TOKEN for CI re-triggering). Access is gated by
# claude-code-action's built-in check — it only acts for commenters with write
# access, so a drive-by "@claude" from an outsider is ignored.

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  pull_request_review:
    types: [submitted]
  issues:
    types: [opened, assigned]

concurrency:
  # Serialize per issue/PR so overlapping @claude mentions don't race on the same
  # branch; never cancel an in-flight resolution (it may be mid-push).
  group: claude-${{ github.event.issue.number || github.event.pull_request.number }}
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: write
  issues: write
  id-token: write
  actions: read

jobs:
  claude:
    # The `issues` arm requires a human author (`user.type == 'User'`): an issue
    # a GitHub App files can carry text the app merely relayed from somewhere
    # else, and this responder holds contents/pull-requests/issues write.
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude')) ||
      (github.event_name == 'issues' && github.event.issue.user.type == 'User' &&
       (contains(github.event.issue.body, '@claude') || contains(github.event.issue.title, '@claude')))
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      # `./.github/actions/claude-run` is a LOCAL composite action, so the runner
      # can only find it after the repository is on disk. Without this step the
      # job died at "Prepare all required actions" on every mention.
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          fetch-depth: 1

      - name: Respond to @claude
        uses: ./.github/actions/claude-run
        with:
          # CLAUDE_CODE_OAUTH_TOKEN fills the LAST tier: it is the account an
          # operator already has, so CI spends every token provisioned for CI
          # first. A tier left unset is skipped, so a setup that configures only
          # that one secret reaches it with no wasted attempt.
          oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK }}
          fallback_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_2 }}
          fallback_oauth_token_2: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_3 }}
          fallback_oauth_token_3: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_4 }}
          fallback_oauth_token_4: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_5 }}
          fallback_oauth_token_5: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN_FALLBACK_6 }}
          fallback_oauth_token_6: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          model: claude-sonnet-4-6
          # This call runs claude-code-action in its event-driven tag mode: the
          # prompt is empty and the action builds its own from the issue or
          # comment, so this composite has nothing to prepend the guard to. The
          # untrusted text here — the mention body — reaches the model through
          # the action's own prompt, which this repo does not compose.
          skip_guard_reason: >-
            Tag mode: claude-code-action builds its own prompt from the event.
          # TEMPLATE_SYNC_TOKEN is optional (see the AUTH note above) — repos
          # that haven't configured it still get a working responder under
          # GITHUB_TOKEN, just without the recursion-guard-evading push identity
          # downstream sync-conflict auto-resolution needs.
          # token-fallback-ok: designed graceful degradation, not an accident.
          github_token: ${{ secrets.TEMPLATE_SYNC_TOKEN || secrets.GITHUB_TOKEN }}
