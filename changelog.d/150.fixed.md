The bundle step now names a module-private binding the resolution left with no reader, and turns auto-merge off. When both sides rename the same thing in opposite directions, a merge can keep one parent's alias and the other parent's callers. Every line then traces to a parent, so no per-line provenance pass sees it, and a test fixture patching that alias patches a name nothing calls.
