- The conflict ledger's disposition rule is now specified in TLA+ and checked by TLC on every run: each conflicted path holds exactly one disposition, a staged, refused or model-bound path is never claimed a second time, and a deferred path is finished only by the pass it names.
