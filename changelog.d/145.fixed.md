- A handoff mark now records WHAT the run ran out of, so the next run on that same commit can tell a repeat from a first refusal.
- A repeat of a shard that spent its whole `SHARD_TIMEOUT_SECONDS` on one hunk gets double that window once. The refusal after it is recorded as a decline, which stops the resolver buying an answer two runs have already given.
- The fan-out's own wall clock is recorded as a distinct cause and never escalated: a longer per-shard window only makes that one worse.
