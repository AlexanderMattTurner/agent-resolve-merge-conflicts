- A leftover-markers refusal names the fault of a shard that could not run, instead of saying only that no reason was recorded.
- The salvage sentence starts its own paragraph, so the last conflicted path's reason no longer runs into it.
- A merge whose index carries conflict markers is refused before the commit, so no partial resolution reaches the push.
