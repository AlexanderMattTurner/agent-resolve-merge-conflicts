- `claude-run`'s credential ladder is now rendered from `lib_credential_ladder.rungs()`, the table the resolve job already walks: its inputs, its backoff/attempt/check/state groups, its execution-log chain and its propagate gate. The input names are unchanged, so every caller and the pinned SHA `merge-delta-review.yaml` reaches it by keep working.
- The free same-credential repeat moves from after the last rung to rung 2, where it re-spends the metered `api_key` on a proven zero-cost failure — the rule the resolve loop already follows. Every caller now fills `api_key`, so that repeat is reachable for each of them.
- The ladder's waits escalate 10, 20, 30, 40, 50, 60, 70 seconds. `claude-run` waited 5 to 75 and the table declared 10 to 90; the one table now sets both.
