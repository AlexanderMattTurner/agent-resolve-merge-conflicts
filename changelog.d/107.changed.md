- The per-file resolution shards and the pre-push merge-delta self-review now run `claude-sonnet-5` instead of `claude-opus-5`. The pre-push pass fixes what it flags and is not the last word on a resolution: the reader that gates a merge is your own post-push one, which this repository now runs on `claude-opus-5`. Pass `model:` to name a different model for the shards; the self-review does not follow it.
