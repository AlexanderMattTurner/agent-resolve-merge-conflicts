- The pre-push merge-delta self-review now runs by default. A caller that names no `self-review` input gets it, so an adopter never pushes a merge that nothing reads. It refuses the push on a finding it cannot fix, and it lands the merge marked unverified when no credential rung answers. Pass `self-review: false` when CI of yours already reads the pushed delta and gates the merge on the finding.
