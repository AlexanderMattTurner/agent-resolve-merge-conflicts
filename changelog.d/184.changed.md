- The merge-delta report's "Deduplicated by the merge" note now covers bash: a function both parents added under one name, which the merged shell file defines once with one parent's own bytes, is named as a forced deduplication rather than left as an unexplained removal. A resolver job without the bash grammar installed writes no such note and does not fail.
