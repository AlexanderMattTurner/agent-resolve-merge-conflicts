- `merge-delta-review.yaml` names the cause when the renderer clone cannot find its commit. A fork that ships this workflow resolves `resolver-ref` to the fork's own commit, which the default `resolver-repository` does not carry; the step said only `reference is not a tree`, and now says which repository lacked the commit and that the fork must name itself.
