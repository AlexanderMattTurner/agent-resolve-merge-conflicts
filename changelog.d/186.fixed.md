- An ending step no longer rewrites the pull request's status comment to "gave up" after a push moved the head past the commit the run resolved. The run's own log line is the whole answer, and the next scan resolves the new head.
