- The contradictory-merge check no longer names a boilerplate line, such as a test's `assert result.returncode == 0`, that two parents each wrote into their own new code. It names a repeated line only when the text between two copies is repeated too, which is what a doubled block has and a shared assertion does not.
