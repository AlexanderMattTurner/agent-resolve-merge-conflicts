- A modify/delete conflict on a path a generation rule claims now resolves as the deletion when nothing committed re-derives it, instead of refusing a conflict whose generator the branch removed along with the file.
- The resolver refuses a resolution whose surviving lines contradict each other: a module-level name one parent added and used that the merged file never reads, and a line the merge base held that no parent still holds and the merge brings back. Every such line traces to a parent, so the merge-delta review passed both and only a test run found them.
