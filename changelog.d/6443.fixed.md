- A pull request GitHub still reports `dirty` after its merge became clean now earns the `merge-conflict` label, so the resolver is dispatched and its push clears GitHub's stale verdict. Each scan also logs one line per pull request, naming the verdict and the arm that decided it.
