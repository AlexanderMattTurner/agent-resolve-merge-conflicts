- A JSON array's conflict is cut entry by entry, so a list both branches rewrote becomes one shard per entry instead of one shard nothing finishes. The cut is bounded, so one long array cannot spend the fan-out budget the other files need, and an array whose entries the two branches reordered or renamed keeps git's own blocks.
